// Types of arrays
// type:1
let fruits = ["apple","mango","banana","grapes"];
console.log(fruits);

// type:2
let numbers = new Array(10); // now it will take 10 as length of the array bcz we've gave it single value
console.log(numbers);

let numbers2 = new Array(2,3,4,6,7); // it will take these as values of the array
console.log(numbers2);

// type:3
let person = Array("muskan","female");
console.log(person);

// type:4
let color = Array.of("pink","orange");
console.log(color);







// ====================================================================================
// TEXT
//we can store different types of data, whether it is reference or primitive